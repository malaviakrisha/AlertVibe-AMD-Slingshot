**PROTOTYPE NOTE**: TFLite integration fully planned. Model-ready.

# Flutter Integration Ready (For alertvibe.tflite)

## pubspec.yaml (Copy this when building)

```yaml
dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.10.4
  permission_handler: ^11.0.1
  flutter_vibrate: ^1.3.0 # Haptic alerts
```
